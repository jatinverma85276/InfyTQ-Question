//Implement a program to find the number of rabbits and chickens in a farm. Given the number of heads and legs of the chickens and rabbits in a farm, identify and
//display the number of chickens and rabbits in the farm.
//If the given input cannot make a valid number of rabbits and chickens, then display an appropriate message.


class Tester {
	public static void main(String[] args) {
		// Implement your code here 
		int head = 150;
		int legs = 500;
		int rabit = 0;
		if (head>legs || legs%2!=0 || head==0){
		    System.out.println("No Solution");
		}else{
		    rabit = legs - 2*head;
		    rabit = rabit/2;
		}
		System.out.println("Chicken : "+(head-rabit));
		System.out.println("Rabbit : "+rabit);
	}
}
